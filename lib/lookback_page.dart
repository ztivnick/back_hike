import 'package:flutter/material.dart';

class LookbackPage extends StatelessWidget {
  const LookbackPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Lookback page")),
    );
  }
}
